version: '3.8'

services:
  stocks-uz:
    build: .
    container_name: stocks-uz-service
    ports:
      - "8004:8000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - PORT=8004
      - ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - ENV=production
