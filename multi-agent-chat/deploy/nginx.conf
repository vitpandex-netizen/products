# Nginx config для Oracle VPS
# Установка: sudo ln -sf /opt/multi-agent-chat/nginx.conf /etc/nginx/sites-available/multi-agent-chat
#            sudo ln -sf /etc/nginx/sites-available/multi-agent-chat /etc/nginx/sites-enabled/
#            sudo certbot --nginx -d chat.yourdomain.com
#            sudo nginx -t && sudo systemctl reload nginx

upstream multi-agent-chat {
    server 127.0.0.1:5555;
    keepalive 64;
}

# HTTP → HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name chat.yourdomain.com;

    # Let's Encrypt verification
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS server
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name chat.yourdomain.com;

    # Let's Encrypt (установить через certbot)
    ssl_certificate /etc/letsencrypt/live/chat.yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chat.yourdomain.com/privkey.pem;

    # Современные TLS настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    # HSTS (включай после проверки)
    add_header Strict-Transport-Security "max-age=63072000" always;

    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy strict-origin-when-cross-origin;

    # Rate limiting на уровне nginx
    limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;
    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;

    # WebSocket support
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 86400s;

    # Статика (кешируется)
    location /styles.css {
        proxy_pass http://multi-agent-chat;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location /app.js {
        proxy_pass http://multi-agent-chat;
        expires 1d;
    }

    location /crypto.js {
        proxy_pass http://multi-agent-chat;
        expires 1d;
    }

    # API
    location /api/ {
        # Auth endpoint — жёсткий лимит
        location /api/auth/ {
            limit_req zone=login burst=3 nodelay;
            proxy_pass http://multi-agent-chat;
        }

        # Остальные API
        limit_req zone=api burst=10 nodelay;
        proxy_pass http://multi-agent-chat;
    }

    # WebSocket
    location /ws {
        proxy_pass http://multi-agent-chat;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Всё остальное
    location / {
        proxy_pass http://multi-agent-chat;
    }

    # Блокировка подозрительных путей
    location ~ /\.(env|git|svn|htaccess|htpasswd) {
        deny all;
        return 404;
    }

    location ~ /(node_modules|data|\.env|\.git) {
        deny all;
        return 404;
    }

    # Лимит размера тела запроса
    client_max_body_size 10m;
}
